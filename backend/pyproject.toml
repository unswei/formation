[project]
name = "formation-backend"
version = "0.1.0"
description = "Formation position service for the soccer field visualiser"
requires-python = ">=3.14"
dependencies = [
  "fastapi>=0.121.0",
  "uvicorn[standard]>=0.38.0",
]

[tool.uv]
package = false
